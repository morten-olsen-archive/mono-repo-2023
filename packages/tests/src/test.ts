import b from '@morten-olsen/template-b';
import c from '@morten-olsen/template-c';

describe('test', () => {
  it('should work', () => {
    expect(b).toBe('ba');
    expect(c).toBe('ca');
  });
});
